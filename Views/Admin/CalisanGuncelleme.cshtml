@using berber.Models;
@model List<Calisan>

@{
    ViewData["Title"] = "Admin Paneli";
    Layout = "~/Views/Shared/_AdminLayout.cshtml";
}


<link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0-alpha1/dist/css/bootstrap.min.css" rel="stylesheet" />


<div class="table-container">
    <table class="table table-bordered">
        <thead class="table-light">
            <tr>
                <th>Çalışan</th>
                <th>Uygunluk Saati</th>
                <th>Uzmanlık Alanları</th>
                <th>Güncelle</th>
            </tr>
        </thead>
        <tbody>
            @foreach (var x in Model)
            {
                <tr>
                    <td>@x.AdSoyad</td>
                    <td>@x.UygunlukSaatleri</td>
                    <td>@x.UzmanlikAlanlari</td>

                    <td>
                        <a href="/Admin/CalisanGuncelle/@x.CalisanID" class="btn-custom-success" style="background-color:lawngreen">Güncelle</a>
                    </td>

                </tr>
            }
        </tbody>
    </table>
</div>